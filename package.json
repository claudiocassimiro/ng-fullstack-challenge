{
  "name": "ng-challenge",
  "version": "1.0.0",
  "description": "desafio full-stack ng challenge",
  "scripts": {
    "install:project": "(cd app && cd backend && npm install && cd .. && cd frontend && npm install)",
    "migrate:dev": "(cd app && cd backend && npx prisma migrate dev)",
    "compose:up": "(cd app && sudo docker-compose up -d --build)",
    "compose:down": "(cd app && docker-compose down --remove-orphans)"
  },
  "author": "Claudio Cassimiro",
  "keywords": [],
  "license": "ISC"
}
